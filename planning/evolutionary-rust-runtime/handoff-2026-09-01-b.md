# Session handoff: Ticket 56 delivered, Ticket 57 next, 2026-09-01

Label: `handoff`
Branch: `pivot/rust-client-core`
Committed HEAD: `db89c9b8 docs(runtime): record import batch delivery`
Worktree: intentionally dirty with only the preserved, uncommitted Ticket 58 host work.

This handoff supersedes [handoff-2026-09-01.md](handoff-2026-09-01.md). Read it completely, then
read the current [map](map.md), [Ticket 57](issues/57-import-atomic-cutover.md), and
[Ticket 58](issues/58-final-web-host-cutover.md) before touching the worktree.

## What this session delivered

Ticket 56 is resolved. Two commits landed:

| Commit | Delivered scope |
| --- | --- |
| `5dbbeec8` | Ticket 56 implementation: durable Runtime Import batch behind its gate. |
| `db89c9b8` | Ticket 56 delivery documentation, Ticket 57 handovers, new Ticket 59, both prior handoffs. |

The implementation passed two independent review axes before commit: six standards review cycles
and two specification review cycles, each cycle with a fresh read-only reviewer, until the final
cycle on each axis approved. The full evidence is in the Ticket 56 resolution comment. Every
correction ran red-green, and the load-bearing tests carry mutation evidence (gutted validator,
removed gate, reset renewal budget each turn a named test red).

## Current worktree: pure Ticket 58

`git status --short` must show exactly 21 modified tracked files and 5 untracked paths, all Ticket
58 host work. The former mixed-ownership files `packages/client-runtime/src/client/index.ts` and
`src/client/client.test.ts` are re-baselined: their Ticket 56 Import hunks are committed in
`5dbbeec8`, so every remaining unstaged hunk in them is Ticket 58 (the `accepted()` helper, the
`updateItem` refactor, the Item and Attachment facade methods, and their tests). No mixed-ownership
staging remains anywhere.

Preserve the aggregate byte-for-byte until Ticket 58 owns it:

- `apps/web/scripts/transitional-reachability.test.ts` and `.ts`
- `apps/web/src/components/vault/item-detail-pane.tsx`, `move-item-dialog.tsx`
- `apps/web/src/lib/crypto.ts`, `runtime-items.ts`, `runtime-items.test.ts`
- `apps/web/src/providers/vault-dnd-provider.tsx`
- `apps/web/src/routes/_app/vaults/{$vaultId/index,favorites,index,tag.$tagName,trash}.tsx`
- `apps/web/tests/e2e/runtime-attachment-move.spec.ts`
- `packages/client-runtime/src/client/index.ts`, `client.test.ts` (Ticket 58 remainder only)
- `packages/client-runtime/src/web/composition.ts`
- `packages/client-runtime/tests/web-attachment-{download-sink,upload}-chromium-harness.ts`,
  `web-attachment-upload.chromium.test.ts`
- `packages/ui/src/components/vault/item-detail/item-attachments.tsx`
- Untracked: `apps/web/src/hooks/use-runtime-item-{attachments,mutations}{,.test}.ts`,
  `packages/client-runtime/src/web/composition-public.test.ts`

Re-run `git status --short` before relying on this list.

## Next work, in order

1. [Ticket 57](issues/57-import-atomic-cutover.md): atomic Server/Web Import cutover. It is
   `ready-for-agent` and unblocked. Read its Comments first: three recorded handovers from Ticket
   56 (authority-contradiction scheduling so a repeated `InvariantViolation` cannot spin,
   the `ImportExecutorPass::RetryScheduled` name on fenced/missing guarded commits, and the missing
   host-observable progress surface — `RuntimeProjection` has no Operation variant yet).
2. [Ticket 58](issues/58-final-web-host-cutover.md): reconcile the preserved host aggregate and
   finish the Web Item/Attachment/lifecycle/projection cutover.
3. [Ticket 28 umbrella](issues/28-remaining-item-write-kinds.md) resolves only after Ticket 58 and
   clean-tree gates.
4. Then Tickets 29, 30, 34 as dependencies allow; resolve the Ticket 38-42 frontiers under the map
   and issue-tracker rules. Do not silently promote a `needs-info` ticket.

[Ticket 59](issues/59-bootstrap-write-order-nondeterminism.md) is new and `needs-info`: the
conformance corpus generator emits prepared-write rows in hash order for multi-Item plans. Its
Decision frontier (does deterministic write order become part of the shared persistence contract,
and does Ticket 38 own it?) needs a maintainer decision, conducted in German, recorded in English.

## Gates and caveats

- `pnpm check:ci:rust` passed at HEAD. All generated-contract `--check` runners, clippy
  `-D warnings`, fmt, and the client-runtime bun suite are green.
- Root `pnpm check:ci` was NOT run to completion: it is expected to stop at the preserved Ticket 58
  Web overlap (Biome and type-check). Do not claim a clean root CI pass until Ticket 58 reconciles
  it.
- Server tests need the development database; the unique-database strategy from
  [handoff-2026-08-31.md](handoff-2026-08-31.md) becomes relevant in Ticket 57.
- The Ticket 57 gate is live in committed code: `runtime/dispatch.rs` skips
  `OperationKind::ImportItems` in the scheduling loop and `runtime/import_executor.rs` carries
  `#[allow(dead_code)]`. Ticket 57 removes both when it wires production dispatch.

## Operating rules (unchanged)

The root agent acts only as orchestrator. One worker at a time (15 GB RAM, no swap, RAM-backed
`/tmp`).

- `CARGO_TARGET_DIR=/home/julian/.cache/bittery-greenfield/cargo-target` for all Rust builds.
- Scratch under `/home/julian/.cache/`, never `/tmp`; never copy the repository.
- Preserve the shared worktree in place: no stash, reset, checkout, discard, mass regeneration, or
  unrelated formatting.
- Writers do not stage or commit. Reviewers are read-only. The root orchestrator owns selective
  staging and commits after both review axes approve.
- Strict red-green TDD at public and persistence seams. Repeat correction plus fresh two-axis
  review until both axes approve; a prior writer never reviews its own work.
- Repository artifacts in English; unresolved maintainer decisions in German, answers recorded in
  English.
- Inspect `git status`, the index, and hunk attribution before and after every staging operation.

The persistent pattern remains: one bounded writer, fresh read-only reviews on both axes,
correction loops until approval, root-owned gates and commits, then a separately reviewed
documentation commit.
