# Desktop production acceptance

Type: task
Status: ready-for-agent
Blocked by: 72
Spec: ../desktop-extension/desktop-acceptance.md

## Contract

Run the [concrete application matrix](../desktop-extension/desktop-acceptance.md): complete behavior
and variants on real Linux Tauri, critical all-category read/write and Account/restart/teardown on
packaged Apple Silicon macOS and Windows x64, plus every platform-specific path. An OS-dependent
discrepancy widens its row. Independent review and simplification address the complete accumulated
Desktop change and the no-duplication condition. Actual existing Extension97 is the native consumer;
requiring migrated76 would violate Desktop-first ordering.

## Acceptance

Record tested revision, commands, real app/OS, observed results and limitations per inventory row. Run required targeted checks plus full pnpm check:ci and pnpm check:ci:rust. Compilation, mocks and a generic browser renderer do not constitute native production acceptance. Missing host evidence remains an explicit blocker; Extension cutover waits.

## Comments

2026-09-23 the maintainer confirmed that only the current Linux workspace is available. Required
packaged macOS Touch ID and Windows Hello acceptance therefore lacks test-host access. Linux
implementation and validation may continue through the complete prerequisite tickets, but Linux
evidence cannot close those platform rows or73, and dependent Extension migration remains gated.
This records an environment blocker, not a change to the accepted matrix.

2026-09-08: dependency-ordered delivery scope recorded. No implementation or acceptance is claimed.

2026-09-09 routine frontier answer: existing66/72/67/91/95/97 contracts fix the behavior and owners.
The focused matrix now closes the acceptance allocation: full Linux Tauri variants, critical
macOS/Windows product histories and every OS-specific credential, biometric, file/menu and native
installation path. Missing hardware/host evidence still blocks completion. Current Linux smoke
and native four-process/source tests are explicitly supporting evidence until joined through real
application gestures. Populated profile upgrade uses actual old source records/credential references;
fresh Sign-in cannot replace it. The twelve rows name evidence, commands and environment boundaries.
Status remains needs-triage pending final coordinating review;72 still blocks implementation and
no application run or capability completion is claimed.

2026-09-09 final independent source/spec review passed: actual harness flags, build wrapper,
release architectures/NSIS and CI scope match the focused acceptance contract. The matrix requires
full real Linux Tauri variants, critical and platform-specific packaged macOS/Windows histories,
real supported biometric hardware and actual old Extension97 integration without a74/76 dependency
cycle.73 is ready with incomplete72 blocking execution. No app run, hardware result or production
acceptance is claimed by this documentation review.
