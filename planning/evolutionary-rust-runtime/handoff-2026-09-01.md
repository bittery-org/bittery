# Session handoff: Ticket 56 standards correction in flight, 2026-09-01

> Historical session snapshot. Use the [current map](map.md#current-delivery-state) and linked
> tickets for status and next steps; worktree conditions and operating instructions below are historical.

Label: `handoff`
Branch: `pivot/rust-client-core`
Committed HEAD: `c692afc2 docs(runtime): record import outcome foundation`
Worktree: intentionally dirty with the complete, uncommitted Ticket 56 implementation and the
preserved Ticket 58 host work carried forward from the 2026-08-31 handoff.

This handoff is the entry point for a fresh orchestrator goal. Read it completely before touching
the worktree, then read the current [map](map.md),
[Ticket 56](issues/56-runtime-import-batch.md), and
[Ticket 58](issues/58-final-web-host-cutover.md).

## Immediate state and first action

Before this handoff was created, `git status --short` reported **38 modified tracked files and 9
untracked paths**. The tracked diff was 2,179 insertions and 341 deletions. This handoff is the
tenth untracked path. The index is intentionally empty and no Ticket 56 commit exists. The
[2026-08-31 handoff](handoff-2026-08-31.md) is itself still untracked; decide whether to commit both
handoffs together during the next documentation commit.

The goal ended because the provider hit its usage limit at 06:31 UTC on 2026-09-01, with new credits
available on 2026-09-07. The interrupted worker was the Ticket 56 **standards correction**. It had
only read source files and had **applied no edit** when the session died, so the worktree still
holds the pre-correction implementation. No build, test, or browser process survived the session.

The completed standards review that triggered the correction found three substantive gaps:

1. Retry uses a 1 ms loop instead of persisted bounded exponential backoff.
2. The Ticket-owned platform-neutral client facade is missing.
3. Several explicit durability and reconciliation histories are unproved.

The orchestrator recorded one constraint on finding 2: the facade addition necessarily shares
`packages/client-runtime/src/client/index.ts` with preserved Ticket 58 work. Only a
**non-overlapping Ticket 56 hunk** is authorized there. The existing Ticket 58 bytes must remain
byte-for-byte unchanged, and the file must be re-baselined as mixed ownership afterward.

The fresh goal must therefore do this first:

1. Assign one fresh strict-TDD **correction writer** for exactly the three findings above, under the
   `client/index.ts` constraint. The writer must not stage or commit.
2. Assign a fresh, read-only **standards reviewer** to review the complete post-correction Ticket 56
   WIP against `c692afc2`, excluding preserved Ticket 58 hunks. It must recheck all three findings
   and the historical classes from the prior handoffs, including ADR 0012 enum generation and
   generated-binding drift.
3. Assign a different fresh, read-only **specification reviewer** to review every Ticket 56
   requirement, test history, gate claim, and acceptance claim from scratch.
4. Repeat correction and both fresh reviews until both axes approve. A prior writer must never
   review its own work.
5. Only after both axes approve may the root orchestrator stage Ticket 56 selectively, run the
   independent gates, commit implementation, write and review the Ticket 56 tracker closeout, and
   commit documentation separately.

Do not treat the green gates in this handoff as approval. They are pre-correction implementation
evidence and one review axis has already rejected the tree.

## Operating rules

The root agent acts only as orchestrator. Use **one worker at a time** because the machine has 15 GB
RAM, no swap, and a RAM-backed `/tmp`.

- Put Rust build output in
  `CARGO_TARGET_DIR=/home/julian/.cache/bittery-greenfield/cargo-target`.
- Put any permitted scratch output under `/home/julian/.cache/`, never `/tmp`, and never copy the
  repository.
- Preserve the shared worktree in place. Do not stash, reset, check out, discard, mass-regenerate,
  or format unrelated files.
- Writers do not stage or commit. Reviewers are read-only. The root orchestrator owns selective
  staging and commits after approval.
- Use strict red-green TDD at public or persistence seams. Run one heavy process at a time.
- Repository artifacts are English. Ask unresolved maintainer decisions in German and record the
  accepted answer in English.
- Inspect `git status`, the index, and hunk attribution before and after every staging operation.
  Ticket 58 bytes must remain unchanged until Ticket 58 owns them.

## Commits completed since the prior handoff

The prior [2026-08-31 handoff](handoff-2026-08-31.md) recorded committed HEAD `31a80c8a`. These are
all later commits in chronological order:

| Commit | Delivered scope |
| --- | --- |
| `08684f43` | Ticket 54 implementation: atomic create-Vault cutover. |
| `cafe5ad3` | Ticket 54 delivery documentation; Ticket 54 resolved. |
| `c14aa81e` | Ticket 55 implementation: Import retained-outcome foundation. |
| `c692afc2` | Ticket 55 delivery documentation; current committed HEAD. |

Tickets 49 through 55 are resolved. Ticket 56 remains `ready-for-agent` in the tracker because its
implementation is uncommitted and not approved.

## Ticket 56 scope and gate decisions

Ticket 56 delivers the durable Runtime Import batch **behind its gate**. The production Import
transport must stay unreachable until Ticket 57 performs the atomic Server/Web cutover:

- `runtime/dispatch.rs` explicitly skips `OperationKind::ImportItems` in the scheduling loop, with a
  comment citing Tickets 56/57. Preserve that gate through every correction.
- `runtime/import_executor.rs` carries `#[allow(dead_code)]` until Ticket 57 wires it into
  production dispatch.
- The platform-neutral facade work from standards finding 2 lands in
  `packages/client-runtime/src/client/index.ts` under the non-overlapping-hunk constraint above.
- Ticket 57 owns the Server route and the Web writer cutover. Ticket 58 owns the remaining host
  reconciliation. Ticket 56 must pre-implement neither.

## Current uncommitted Ticket 56 implementation

The aggregate currently implements, pre-correction:

- three new modules in `bittery-client-core`:
  `runtime/import.rs` (batch acceptance), `runtime/import_executor.rs` (gated executor), and
  `runtime/import_tests.rs` (import history tests);
- `runtime.rs` wiring for the new modules and dispatch of `RuntimeRequest::ImportItems`;
- the dispatch-loop gate that keeps production Import transport unreachable until Ticket 57;
- protocol additions `ImportItems`, `ImportItemDraft`, and `ImportBatchAccepted` in `protocol.rs`,
  with matching changes in `lib.rs`, `runtime/outcome.rs`, `replica.rs`, `replica/domain.rs`,
  `replica/persistence_contract.rs`, and `replica/replica_conformance.rs`;
- regenerated cross-language artifacts under ADR 0012: `bittery-client-bindings/src/lib.rs`, the
  generated Kotlin and Swift bindings, `contract.schema.json`, `contract.ts`, `validator.js`, and
  `history-corpus.json`.

Known defects in this aggregate are exactly the three standards findings above.

## Review and correction history

### Initial standards review: rejected

Completed 2026-09-01 06:30 UTC. Findings: the 1 ms retry loop instead of persisted bounded
exponential backoff, the missing platform-neutral client facade, and unproved durability and
reconciliation histories. The specification review was never spawned.

### Correction 1: interrupted before any edit

The correction writer started 06:30 UTC and died at the provider usage limit at 06:31 UTC while
still reading `import_tests.rs` and related runtime source. The worktree is unchanged since the
rejected review. The next goal begins with this correction.

## Latest green evidence

These are the latest captured results for the pre-correction tree, reverified on 2026-09-01 after
the session died:

- `cargo check -p bittery-client-core`: passed.
- `cargo test -p bittery-client-core import`: 9/9 import tests passed.
- Generated Server-contract import-outcome test: 1/1.

Root `pnpm check:ci` and `pnpm check:ci:rust` were not rerun after the Ticket 55 commit landed.
Expect root CI to stop at the preserved Ticket 58 Web Biome and type-check overlap described in the
[2026-08-31 handoff](handoff-2026-08-31.md); do not claim a clean root CI pass until Ticket 58
reconciles it. The safe unique-database Server test strategy in that handoff is unchanged and
becomes relevant again in Ticket 57.

## Preserved Ticket 58 work

Ticket 58 is intentional unfinished host work. Preserve these tracked files or their Ticket 58
hunks byte-for-byte:

- `apps/web/scripts/transitional-reachability.test.ts`
- `apps/web/scripts/transitional-reachability.ts`
- `apps/web/src/components/vault/item-detail-pane.tsx`
- `apps/web/src/components/vault/move-item-dialog.tsx`
- `apps/web/src/lib/crypto.ts`
- `apps/web/src/lib/runtime-items.test.ts`
- `apps/web/src/lib/runtime-items.ts`
- `apps/web/src/providers/vault-dnd-provider.tsx`
- `apps/web/src/routes/_app/vaults/$vaultId/index.tsx`
- `apps/web/src/routes/_app/vaults/favorites.tsx`
- `apps/web/src/routes/_app/vaults/index.tsx`
- `apps/web/src/routes/_app/vaults/tag.$tagName.tsx`
- `apps/web/src/routes/_app/vaults/trash.tsx`
- `apps/web/tests/e2e/runtime-attachment-move.spec.ts`
- `packages/client-runtime/src/client/client.test.ts`
- `packages/client-runtime/src/client/index.ts`
- `packages/client-runtime/src/web/composition.ts`
- `packages/client-runtime/tests/web-attachment-download-sink-chromium-harness.ts`
- `packages/client-runtime/tests/web-attachment-upload-chromium-harness.ts`
- `packages/client-runtime/tests/web-attachment-upload.chromium.test.ts`
- `packages/ui/src/components/vault/item-detail/item-attachments.tsx`

The `transitional-reachability` scripts are new to this list relative to 2026-08-31. Their current
diffs remove Ticket 28 holdout entries for host files the Ticket 58 aggregate migrated; audit their
hunks before any staging decision touches them.

These untracked paths are also Ticket 58 work:

- `apps/web/src/hooks/use-runtime-item-attachments.test.ts`
- `apps/web/src/hooks/use-runtime-item-attachments.ts`
- `apps/web/src/hooks/use-runtime-item-mutations.test.ts`
- `apps/web/src/hooks/use-runtime-item-mutations.ts`
- `packages/client-runtime/src/web/composition-public.test.ts`

These untracked paths belong to Ticket 56 and must be included only after approval:

- `packages/client-runtime/crates/bittery-client-core/src/runtime/import.rs`
- `packages/client-runtime/crates/bittery-client-core/src/runtime/import_executor.rs`
- `packages/client-runtime/crates/bittery-client-core/src/runtime/import_tests.rs`

After the facade correction lands, `packages/client-runtime/src/client/index.ts` and its test file
become mixed ownership and require hunk-level staging: the new Ticket 56 facade hunk plus preserved
Ticket 58 Item and Attachment facade work. Re-baseline the Ticket 58 manifest accordingly.

Re-run `git status --short` before relying on this list. The worktree is shared state and the fresh
goal must account for every changed and untracked path before staging.

## Ticket 56 completion procedure after approval

1. Root reruns the narrow reviewer-requested tests, `contracts:check`, `pnpm check:ci:rust`, and
   `git diff --check`. Root records the honest root-CI Ticket 58 caveat.
2. Assign a staging-only worker to inventory all paths and stage only Ticket 56, using selective
   index patches for the mixed `client/index.ts`. It must report cached files, preserved unstaged
   hunks, and `git diff --cached --check`; worktree bytes must remain unchanged.
3. Root audits the cached diff for Ticket 58 leakage and commits Ticket 56 implementation.
4. Assign a docs-only writer to mark Ticket 56 resolved, record exact evidence and commit, and
   promote Ticket 57 only if tracker policy and dependencies permit. Review those docs with a fresh
   read-only reviewer before root commits them.

## Remaining ticket order

Continue the dependency chain in this order:

1. [Ticket 56](issues/56-runtime-import-batch.md): finish the standards correction, fresh two-axis
   review, implementation commit, and documentation closeout.
2. [Ticket 57](issues/57-import-atomic-cutover.md): atomic Server/Web Import cutover.
3. [Ticket 58](issues/58-final-web-host-cutover.md): reconcile the preserved host aggregate and
   finish Web Item/Attachment/lifecycle/projection cutover.
4. Resolve the [Ticket 28 umbrella](issues/28-remaining-item-write-kinds.md) only after Ticket 58 and
   clean-tree gates are complete.
5. Continue [Ticket 29](issues/29-rotation-operation-outcomes.md), then
   [Ticket 30](issues/30-runtime-owned-live-sync.md), then
   [Ticket 34](issues/34-web-sqlite-opfs-prototype.md) as their dependencies allow.
6. Resolve the frontiers and dependency order for [Ticket 38](issues/38-replica-persistence-evolution.md)
   through [Ticket 42](issues/42-browser-replica-recovery.md) under the map and issue-tracker rules.
   Do not silently promote a `needs-info` ticket.

The persistent operating pattern remains: one bounded writer, one fresh read-only review, correction
loops until approval, root-owned gates and commits, then a separately reviewed documentation commit.
