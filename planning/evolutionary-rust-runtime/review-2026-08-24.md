# First-slice adversarial review, 2026-08-24

Ticket: [23](issues/23-first-slice-adversarial-review.md)
Fixed point: `legacy-v0.5.2` (`f021c85e1d3a9d3f3418ba67a9ff04f319987903`)
Reviewed head: `5f014624`

## Result

The original review filed the missing three-adapter conformance and browser reconciliation path as
tickets [31](issues/31-shared-replica-adapter-conformance.md) and
[32](issues/32-first-slice-end-to-end-acceptance.md). The first rerun confirmed both implementations
and their empty-Vault protocol blocker, then filed the narrower proof gaps as tickets
[36](issues/36-web-offline-authority-read.md) and
[37](issues/37-sqlite-complete-failure-matrix.md). Both landed. Fresh independent Standards and Spec
reviews over `1d60d73c...5f014624` found no remaining issue, so ticket 23 is resolved with zero open
correctness or security blockers.

## Criterion map

| Criterion | Evidence | Verdict |
| --- | --- | --- |
| Unchanged crypto algorithms, formats, AAD, and key hierarchy | Existing crypto-core remains the implementation; format/vector and Rust gates passed. No reviewer found algorithm or persisted-format drift. | Pass |
| No plaintext persistence or logging | Replica/Operation records carry ciphertext; causal tests cover generated plans, both durable adapters, PostgreSQL diagnostics, actual Server output, and browser console. | Pass |
| Explicit Account scope and one Runtime owner | Closed requests carry Account IDs; Active account remains host-side selection. The whole-entry-graph Web audit passes and finds no reachable transitional read/create or Sync owner. | Pass for first-slice paths |
| Durable accept, no finite retry, no discard | Rust accept/dispatch tests cover atomic acceptance and seven attempts; dispatch has no attempt terminal state or discard request. | Pass in focused tests |
| Immutable outcome and exactly-once reconciliation | Runtime and Server suites cover fingerprint matching, retained outcomes, lookup, authoritative fetch, receipt, and atomic reconciliation. | Pass in focused tests |
| Shared Replica conformance | One Rust-generated corpus replays through in-memory, SQLite, and IndexedDB adapters. Both durable adapters cover Install, accepted Commit, reconciliation Commit, and Lock failures; SQLite reopens and byte-compares the complete Account at every statement boundary. | Pass |
| Server atomic Operation outcome | Server tests and both CI gates pass; response-cache execution remains only in the five Rotation routes assigned to ticket 29. | Pass for Item routes |
| Complete browser acceptance scenario | Playwright drives durable offline accept through reconciliation and separately proves that a fresh profile bootstraps encrypted authority, replaces its Worker, restores locked, unlocks online, disconnects transport, recreates observation, and renders the same Item without a Server response or legacy authority. | Pass |
| Generated contract drift | Runtime protocol, transport, persistence, Server contract, native binding, and combined-WASM drift checks passed in the full gates. | Pass |
| No second Worker or reachable dual writer | Web composition creates one combined Runtime Worker. The remaining crypto-port Worker belongs to transitional Desktop/Mobile roots; Web transitional Sync is unreachable. | Pass |

## Reviewer claims checked

The Standards review found no hard documented-standard breach. Its source-text-test claims are real:
`runtime-session.test.ts` and parts of `runtime-items.test.ts` match implementation identifiers and
hand-picked files. They do not replace behavioral coverage. The ownership blind spot is already
closed by `web-import-graph.ts` plus `transitional-reachability.test.ts`; the missing product-path
coverage is captured by ticket 32. Duplicated generator and existing-host lifecycle code are
non-blocking design judgements outside this slice.

The rerun found no hard Standards violation. Its stale Server comment and one JSON-token corpus
assertion are non-blocking style observations because structured behavioral coverage exists. The
Spec review's narrower offline-read and SQLite failure-matrix claims were confirmed by the E2E
inventory and adapter tests and are filed as tickets 36 and 37.

The final delta review found no documented-standard or Spec issue. Ticket 36's initial and first
review failures were test-harness defects, not product defects; its corrected test observes real
Worker protocol, exact owner count, encrypted durable state, UI output, and the absence of any
post-disconnect Server response without deciding ticket 30's reconnect attempts. Ticket 37's
Domain-recording fixture and reopened SQLite comparisons received a clear review with neither a
product nor fixture defect.

## Commands

Run from a clean `pivot/rust-client-core` tree with
`DATABASE_URL=postgresql://postgres:password@localhost:5436/bittery` and `SQLX_OFFLINE=true`:

- `git diff legacy-v0.5.2...HEAD`
- `pnpm check:ci` — passed
- `pnpm check:ci:rust` — passed; client-core reported 185 unit, 3 corpus, and 3 Server-contract tests
- targeted first-slice Playwright scenario — passed
- targeted post-restart offline-authority Playwright scenario — passed
- Web transitional reachability audit — 11 tests passed
- focused `rg` searches for SQLite adapters, E2E acceptance terms, finite retry/discard, Active
  account scope, plaintext/logging sinks, Worker construction, and idempotency execution call sites
