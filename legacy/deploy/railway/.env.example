# Caddy service — reference variables for Railway private networking
SERVER_PRIVATE_DOMAIN=${{server.RAILWAY_PRIVATE_DOMAIN}}
SERVER_PORT=${{server.PORT}}
WEB_PRIVATE_DOMAIN=${{web.RAILWAY_PRIVATE_DOMAIN}}
WEB_PORT=8080

# Optional: comma-separated Caddy plugins passed to xcaddy in build.sh
# CADDY_PLUGINS=
