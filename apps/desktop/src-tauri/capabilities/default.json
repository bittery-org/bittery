{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "client-runtime:default",
    "opener:default",
    "biometry:default",
    "store:default",
    "core:window:default",
    "core:window:allow-start-dragging"
  ]
}
