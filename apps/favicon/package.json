{
	"name": "favicon",
	"main": "src/index.ts",
	"type": "module",
	"scripts": {
		"build": "tsdown",
		"check-types": "tsc -b",
		"dev": "bun run --hot src/index.ts",
		"start": "bun run dist/index.mjs"
	},
	"dependencies": {
		"@bittery/favicon": "workspace:*",
		"dotenv": "catalog:",
		"hono": "catalog:"
	},
	"devDependencies": {
		"@bittery/config": "workspace:*",
		"@types/bun": "^1.2.6",
		"tsdown": "^0.16.5",
		"typescript": "^5.8.2"
	}
}
