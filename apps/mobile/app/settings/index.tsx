import { SettingsScreen } from "@/screens/settings-screen";

/** The stack route the account sheet deep-links to. */
export default function SettingsStackScreen() {
	return <SettingsScreen presentation="stack" />;
}
